!someObject = {
    array: int[*]
    other: {
        hello: string
        firstColor: color
        secondColor: color
    }
}

nullableBoolArr: boolean?[*]

test: [
    int
    !someObject
    float?
    boolean?
]

objectWithMoreKeys: {
    newKey: int?[*]?
    myBool: boolean
    vertices: vec2?[4]
    myFloat: float
    ...!someObject
}